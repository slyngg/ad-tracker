# Required
DB_PASSWORD=changeme
JWT_SECRET=change-this-to-a-random-secret
NODE_ENV=production

# AI Operator (required for /operator chat)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Facebook / Meta Ads (configure here or via Settings > Connections)
FB_ACCESS_TOKEN=your-facebook-api-token
FB_AD_ACCOUNT_IDS=act_123456789,act_987654321

# Webhook HMAC secrets (configure here or via Settings > Connections)
CC_WEBHOOK_SECRET=your-checkout-champ-secret
SHOPIFY_WEBHOOK_SECRET=your-shopify-secret

# CORS: comma-separated allowed origins (leave empty for same-origin in production)
ALLOWED_ORIGIN=

# Dashboard URL (used in Slack bot responses)
DASHBOARD_URL=https://optic-data.com

# Production only (used by deploy/docker-compose.prod.yml)
DOMAIN=dash.example.com

# Slack Bot (optional — leave empty to disable)
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_SIGNING_SECRET=your-slack-signing-secret
SLACK_APP_TOKEN=xapp-your-slack-app-token

# Checkout Champ API polling (optional)
CC_API_KEY=
CC_API_URL=

# Legacy (deprecated — use JWT auth instead)
AUTH_TOKEN=
